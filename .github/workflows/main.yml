name: "Docker Build
on:
  pull_request:
    - main
  push:
    - main
jobs:
    runs-on: ubuntu-latest
    steps:
      - name:  Check out the repository to the runner
        uses: actions/checkout@v4
      - name: Docker Build
        run: |
          docker build -t name chatgpt:latest .
